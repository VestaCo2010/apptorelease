#include "OpenDirectory.h"

using namespace VOTE::Util;

void OpenDirectory::showDirectory(QString address)
{
    //    QStringList args;
    //    args << "/select," << QDir::toNativeSeparators(address);
    //    QProcess *process = new QProcess();
    //    process->start("explorer.exe", args);
}
